spring:

  h2:
    console:
      enabled: true
      path: "/console"

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:localtest;MODE=DB2
    platform: local